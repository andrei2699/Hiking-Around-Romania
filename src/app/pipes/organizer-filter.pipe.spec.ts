import { OrganizerFilterPipe } from './organizer-filter.pipe';

describe('OrganizerFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new OrganizerFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
